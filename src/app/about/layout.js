

export default function Layout({ children }) {
    return (
        <div>
            <h1>Common Layout Apply on about page and its nested roots</h1>
            {
                children
            }
        </div>
    )
}